import styled from 'styled-components'

export const Item = styled.li`
  margin: var(--gap-smallest) calc(var(--gap-smallest) / 2) 0;
`

export const ListTagsStyle = styled.ul`
  display: flex;
  flex-wrap: wrap;
  align-items: center;
`
