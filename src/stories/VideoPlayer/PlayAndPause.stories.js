import React from 'react'
import { PlayAndPause } from '../../lib'

export default {
  title: 'Components/VideoPlayer/PlayAndPause',
  component: PlayAndPause
}

const Template = (args) => <PlayAndPause {...args} />

export const Default = Template.bind({})
